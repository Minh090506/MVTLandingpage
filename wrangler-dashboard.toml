name = "mvt-dashboard"
main = "dashboard-worker.js"
compatibility_date = "2024-01-01"

# Custom domain: googlead.myvivatour.com
# After first deploy, add custom domain via Cloudflare Dashboard:
# Workers & Pages → mvt-dashboard → Settings → Domains & Routes → Add Custom Domain
