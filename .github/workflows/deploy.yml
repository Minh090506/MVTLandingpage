name: Build & Deploy MVT Landing Pages

on:
  push:
    branches: [main]
    paths:
      - 'pages/**'
      - 'build.js'
      # build.js inlines these into worker.js, so editing one changes what ships
      - 'worker-modules/**'
      - 'wrangler.toml'
      - 'wrangler-dental.toml'
      - 'dashboard.html'
      - 'build-dashboard.js'
      - 'wrangler-dashboard.toml'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:  # Allow manual trigger
    inputs:
      skip_validation:
        description: 'Emergency only: deploy without the landing-page validation gate'
        required: false
        type: boolean
        default: false

jobs:
  build-and-deploy:
    name: Build & Deploy to Cloudflare Workers
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate landing pages
        if: github.event_name != 'workflow_dispatch' || inputs.skip_validation != true
        run: node scripts/validate-landing-pages.js

      - name: Record emergency validation bypass
        if: github.event_name == 'workflow_dispatch' && inputs.skip_validation == true
        run: |
          echo "## ⚠️ Validation bypassed" >> "$GITHUB_STEP_SUMMARY"
          echo "This manual deployment skipped the landing-page validation gate." >> "$GITHUB_STEP_SUMMARY"

      - name: Build worker.js from pages
        run: node build.js

      - name: Deploy Landing Pages Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

      - name: Build dashboard-worker.js
        run: node build-dashboard.js

      - name: Deploy Dashboard Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy -c wrangler-dashboard.toml

      - name: Deploy Dental Worker (unified worker.js under vietnamdentaltravel name)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy -c wrangler-dental.toml

      - name: Deployment summary
        run: |
          echo "## ✅ Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Dental (implant.vietnamdentaltravel.com)" >> $GITHUB_STEP_SUMMARY
          echo "**Worker:** vietnamdentaltravel" >> $GITHUB_STEP_SUMMARY
          echo "- https://implant.vietnamdentaltravel.com/" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Landing Pages (escape.myvivatour.com)" >> $GITHUB_STEP_SUMMARY
          echo "**Worker:** escape-myvivatour" >> $GITHUB_STEP_SUMMARY
          for dir in pages/*/; do
            page=$(basename "$dir")
            if [ "$page" = "escape" ]; then
              echo "- **$page** → https://escape.myvivatour.com/" >> $GITHUB_STEP_SUMMARY
            else
              echo "- **$page** → https://escape.myvivatour.com/$page" >> $GITHUB_STEP_SUMMARY
            fi
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Dashboard (googlead.myvivatour.com)" >> $GITHUB_STEP_SUMMARY
          echo "**Worker:** mvt-dashboard" >> $GITHUB_STEP_SUMMARY
          echo "- **Dashboard** → https://googlead.myvivatour.com/" >> $GITHUB_STEP_SUMMARY

  upload-images:
    name: Upload images to Supabase Storage
    runs-on: ubuntu-latest
    needs: build-and-deploy
    if: |
      contains(github.event.head_commit.message, '[upload-images]') ||
      github.event_name == 'workflow_dispatch'
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'  # 22+ has native WebSocket required by @supabase/supabase-js

      - name: Install dependencies
        run: npm install @supabase/supabase-js

      - name: Upload images to Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: node scripts/upload-to-supabase.js
